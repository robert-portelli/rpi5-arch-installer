# /etc/sysctl.d/99-vm-zram.conf

# Prefer zram over page cache reclaim
vm.swappiness = 130

# Disable swap I/O clustering (RAM-backed swap)
vm.page-cluster = 0

# Start reclaim earlier and smoother
vm.watermark_scale_factor = 125

# Avoid emergency reclaim bursts
vm.watermark_boost_factor = 0
